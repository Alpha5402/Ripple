# 工作区恢复与增量索引验收

2026-09-13，隔离测试目录和浏览器配置，不读写用户笔记。

- Web + 真实 WeMM：初次索引 2 篇笔记后完整关闭、重启 Chromium，恢复同一工作区及 2 个向量，恢复阶段对模型服务请求为 0。新增章节后重新选择快照源目录同步，总片段变为 3，最近工作区仍只有 1 条。
- Electron + 真实 WeMM：不带 `--vault` 重启恢复最后目录；已有 2 个片段可用。外部新增章节触发文件监听，报告编码 1、复用 1，覆盖变为 3 个片段。
- 浏览器真实目录句柄：使用 OPFS 测试目录验证 IndexedDB 序列化、刷新恢复、自动新增/删除检测和 isSameEntry 去重。该用例验证浏览器 API 路径，未将 OPFS 冒充 macOS 外部目录；实际磁盘监听由上面的 Electron 用例覆盖。
- 两个标签页：较新页面保存 Lens=67，旧页面尝试保存 33 被拒绝，界面显示冲突，缓存仍为 67。
- 原生认证测试使用本地协议替身和无价值测试 Key：磁盘单独的密文不含 Key 原文；普通配置不含 Key；重启后无需重新输入即可完成新增片段请求。请求共 3 次：固定连接测试、初始正文、新增章节。
- 实际 macOS 打包应用：关闭后不带任何参数启动，自动恢复 2 篇测试笔记和 1 条最近工作区记录。工作区切换的草稿确认可返回编辑并保留未保存内容。
- 96 项回归通过；包含规范路径去重、重启零请求、改变片段复用、新增/删除、自动更新开关持久化、浏览器认证缓存不保存 Key。

浏览器文件上传降级模式为目录快照，无法后台读取源目录；需要点击「同步目录」重新选择来源。目录访问 API 的授权失效时保留缓存，等待重新授权。浏览器 Key 仅存会话内；关闭后可读缓存，但认证模型增量更新需要再次输入。浏览器站点数据清理会删除缓存。
