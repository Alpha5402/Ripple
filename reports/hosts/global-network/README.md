# 全局知识网络验收

2026-09-13：使用独立合成目录，100 篇笔记形成 90 条显式关系，另含 10 个孤立节点。

- Web：显示 100 个节点、90 条去重连线；Lens 筛选不删除节点；跨中心关系证据可读取；孤立笔记可预览并进入局部探索，再返回全局。无页面 JavaScript 错误。
- macOS：直接启动 `release/current/Ripple-darwin-arm64/Ripple.app`，确认 `app.isPackaged=true`，同样显示 100 个节点、90 条关系。
- 图标：打包后的 ICNS 与构建源 SHA-256 一致；应用运行时能读取 R 标记 PNG，并显式设置 Dock 图标。未对其他旧副本或 macOS 图标缓存作修改。
- 92 项回归通过，核心与 UI 类型检查、核心/Web/桌面构建及公开文件审计通过。

全局网络汇总每篇笔记已发现的关系，并遵守隐藏关系设置；语义关系仍受模型索引、候选检索预算限制，不代表所有笔记两两都建立连线。此次视觉运行验收规模为 100 篇。

截图只含测试笔记：`web.png`、`desktop.png`。
